<div class="container p-5">
  <div class="row">
    <div class="col-md-10 offset-md-1">

      <h2 class="mb-4 px-4 bg-light text-center text-nowrap d-inline-block">ログイン</h2><br>

      <p class="mb-4 px-4 bg-light text-center text-nowrap d-inline-block">会員の方はこちらからログイン</p>

      <%= form_with model: @customer, url: customer_session_path do |f| %>

        <%= render "public/shared/error_messages", resource: resource %>

        <table class="table table-borderless text-nowrap">
          <tbody>
            <tr>
              <td><%= f.label "メールアドレス" %></td>
              <td><%= f.email_field :email, autocomplete: "email", placeholder: "sample@example.com" %></td>
            </tr>
            <tr>
              <td><%= f.label "パスワード" %></td>
              <td><%= f.password_field :password, autocomplete: "current-password" %></td>
            </tr>
            <tr>
              <td></td>
              <td><%= f.submit "ログイン", class: "btn btn-primary" %></td>
            </tr>
          </tbody>
        </table>
      <% end %>

      <div>
        <p class="mb-4 px-4 bg-light text-center text-nowrap d-inline-block">会員登録がお済みでない方</p>
      </div>

      <div class="text-nowrap d-inline">
        <%= render "public/shared/links" %>
        <p>から新規登録を行ってください。</p>
      </div>

    </div>
  </div>
</div>